<?xml version="1.0" encoding="UTF-8"?>
<ContentPage Title="Wyszukiwarka" xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="EngineerProject.Mobile.Views.Home.Pages.SearchPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Wyloguj" Clicked="OnLogout" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout Margin="5">
            <Entry x:Name="SearchBarEntry" TextChanged="SearchBarTextChanged" Placeholder="Wpisz wyszukiwaną frazę"></Entry>
            <ListView x:Name="GroupsList" ItemTapped="OnGroupSelect">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Name}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>